
<div class="topic-index-wrapper">
    <div class="container">
        <div class="row">
            <h2>検索結果</h2>
            <% @topics.each do |topic| %>
                <p>投稿者「<%= topic.user.name %>」</p>
                <p> 料理名「<%=topic.title%>」</p>
                <p>業態 「<%=topic.subject%>」</p>
                <p><%= image_tag topic.image.url %></p>
            <%end%>
        </div>
    </div>
</div>